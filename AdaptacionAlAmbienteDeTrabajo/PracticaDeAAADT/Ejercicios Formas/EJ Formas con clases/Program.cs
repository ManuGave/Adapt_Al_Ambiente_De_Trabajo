/*

namespace PracticaDeAAADT;

public class Program
{
    static void Main()
    {
        // Ejemplo de inicialización en Verde

        

        Console.WriteLine("--- Inicio ---");

        List<Figuras> formas = new List<Figuras>();
        
        formas.Add (new Cuadrado(4));
        formas.Add (new Triangulo(2,2));
        formas.Add (new Cuadrado(2));
        formas.Add (new Rectangulo(4, 4));

        foreach (Figuras forma in formas)
        {
            Console.WriteLine(forma.mostrarDatos());
        }
    }
}

*/